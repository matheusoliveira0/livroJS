const dias = Number(prompt("Informe a quantidade de dias:")); // lê os dados de entrada
const horas = Number(prompt("Informe a quantidade de horas:"));

const total = dias * 24 + horas; // calcula a duração

alert(`Total de horas: ${total}`); //informa o resultado
